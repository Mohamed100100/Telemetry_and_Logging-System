# Phase 6: System Wrap-Up (Design & Configuration)

## 📋 Table of Contents

1. [Overview](#-overview)
2. [Goals](#-goals)
3. [Design Pattern: Façade](#-design-pattern-façade)
4. [Architecture](#-architecture)
5. [Configuration System](#-configuration-system)
6. [Class Diagram](#-class-diagram)
7. [Component Descriptions](#-component-descriptions)
8. [System Flow](#-system-flow)
9. [Initialization Sequence](#-initialization-sequence)
10. [Main Loop Flow](#-main-loop-flow)
11. [Shutdown Sequence](#-shutdown-sequence)
12. [Build & Run Instructions](#-build--run-instructions)
13. [Usage Examples](#-usage-examples)
14. [Key Learning Points](#-key-learning-points)
15. [Common Pitfalls](#-common-pitfalls)
16. [File Structure](#-file-structure)
17. [Complete System Integration](#-complete-system-integration)
18. [Summary](#-summary)

---

## 📋 Overview

Phase 6 is the **final integration phase** that wraps the entire Telemetry and Logging System into a clean, configurable, and user-friendly package. It implements the **Façade Pattern** to hide all internal complexity behind a simple interface, and provides **JSON-based runtime configuration** for maximum flexibility.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PHASE 6: THE BIG PICTURE                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                              BEFORE PHASE 6                                 │
│                              ══════════════                                 │
│                                                                             │
│    User must manually create and connect:                                   │
│                                                                             │
│    ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│    │LogManager│ │ThreadPool│ │RingBuffer│ │ Sources  │ │  Sinks   │         │
│    └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘         │
│         │            │            │            │            │               │
│    ┌────▼────────────▼────────────▼────────────▼────────────▼────┐          │
│    │                    50+ Lines of Setup Code                  │          │
│    └─────────────────────────────────────────────────────────────┘          │
│                                                                             │
│ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│                                                                             │
│                              AFTER PHASE 6                                  │
│                              ═════════════                                  │
│                                                                             │
│    User only needs:                                                         │
│                                                                             │
│    ┌─────────────────────────────────────────────────────────────┐          │
│    │                                                             │          │
│    │     TelemetryApp app("config.json");                        │          │
│    │     app.start();                                            │          │
│    │                                                             │          │
│    │                    2 Lines of Code!                         │          │
│    │                                                             │          │
│    └─────────────────────────────────────────────────────────────┘          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Goals

| Goal | Description |
|------|-------------|
| **Simplify Usage** | Hide all complexity behind a simple interface |
| **Enable Configuration** | Allow runtime configuration via JSON files |
| **Integrate All Phases** | Combine Phases 1-5 into a cohesive system |
| **Apply Façade Pattern** | Provide a unified interface to subsystems |

---

## 🏛️ Design Pattern: Façade

The **Façade Pattern** provides a simplified interface to a complex subsystem. It doesn't add new functionality but makes the system easier to use.

### Pattern Structure

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FAÇADE PATTERN                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                              WITHOUT FAÇADE                                 │
│                              ══════════════                                 │
│                                                                             │
│         Client                                                              │
│           │                                                                 │
│           ├────────────────┬────────────────┬────────────────┐              │
│           │                │                │                │              │
│           ▼                ▼                ▼                ▼              │
│    ┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐        │
│    │ Subsystem  │   │ Subsystem  │   │ Subsystem  │   │ Subsystem  │        │
│    │     A      │   │     B      │   │     C      │   │     D      │        │
│    └────────────┘   └────────────┘   └────────────┘   └────────────┘        │
│                                                                             │
│    Client must know about ALL subsystems and how to use each one!           │
│                                                                             │
│ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│                                                                             │
│                               WITH FAÇADE                                   │
│                               ═══════════                                   │
│                                                                             │
│         Client                                                              │
│           │                                                                 │
│           │  Simple Interface                                               │
│           ▼                                                                 │
│    ┌─────────────────────────────────────────────────────────────┐          │
│    │                        FAÇADE                               │          │
│    │                    (TelemetryApp)                           │          │
│    │                                                             │          │
│    │    ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │          │
│    │    │Subsystem │  │Subsystem │  │Subsystem │  │Subsystem │   │          │
│    │    │    A     │  │    B     │  │    C     │  │    D     │   │          │
│    │    └──────────┘  └──────────┘  └──────────┘  └──────────┘   │          │
│    │                                                             │          │
│    └─────────────────────────────────────────────────────────────┘          │
│                                                                             │
│    Client only knows about Façade - complexity is hidden!                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Façade Applied to TelemetryApp

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    TELEMETRYAPP AS FAÇADE                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                           ┌─────────────┐                                   │
│                           │    User     │                                   │
│                           └──────┬──────┘                                   │
│                                  │                                          │
│                                  │ app("config.json")                       │
│                                  │ app.start()                              │
│                                  │ app.stop()                               │
│                                  ▼                                          │
│    ┌─────────────────────────────────────────────────────────────────────┐  │
│    │                                                                     │  │
│    │                        TelemetryApp                                 │  │
│    │                         (FAÇADE)                                    │  │
│    │                                                                     │  │
│    │   ┌───────────────────────────────────────────────────────────┐     │  │
│    │   │                   HIDDEN COMPLEXITY                       │     │  │
│    │   │                                                           │     │  │
│    │   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │     │  │
│    │   │  │ ConfigLoader│  │  LogManager │  │ ThreadPool  │        │     │  │
│    │   │  │  (Phase 6)  │  │  (Phase 4)  │  │  (Phase 4)  │        │     │  │
│    │   │  └─────────────┘  └─────────────┘  └─────────────┘        │     │  │
│    │   │                                                           │     │  │
│    │   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │     │  │
│    │   │  │ RingBuffer  │  │ LogFormatter│  │   Sources   │        │     │  │
│    │   │  │  (Phase 4)  │  │  (Phase 3)  │  │  (Phase 2)  │        │     │  │
│    │   │  └─────────────┘  └─────────────┘  └─────────────┘        │     │  │
│    │   │                                                           │     │  │
│    │   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │     │  │
│    │   │  │    Sinks    │  │    RAII     │  │   SOME/IP   │        │     │  │
│    │   │  │  (Phase 1)  │  │  (Phase 2)  │  │  (Phase 5)  │        │     │  │
│    │   │  └─────────────┘  └─────────────┘  └─────────────┘        │     │  │
│    │   │                                                           │     │  │
│    │   └───────────────────────────────────────────────────────────┘     │  │
│    │                                                                     │  │
│    └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Façade Benefits

| Benefit | Description |
|---------|-------------|
| **Simplicity** | Simple interface hides complex subsystem interactions |
| **Decoupling** | Client code doesn't depend on subsystem classes directly |
| **Flexibility** | Subsystem can change without affecting clients |
| **Organization** | Single entry point for the entire application |
| **Testability** | Easier to mock/test the façade interface |

---

## 🏗️ Architecture

### Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       COMPLETE SYSTEM ARCHITECTURE                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         CONFIGURATION LAYER                         │   │
│  │                                                                     │   │
│  │   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐   │   │
│  │   │   JSON      │────────▶│ ConfigLoader│────────▶│  AppConfig  │   │   │
│  │   │   File      │  parse  │             │ create  │   Struct    │   │   │
│  │   └─────────────┘         └─────────────┘         └──────┬──────┘   │   │
│  │                                                          │          │   │
│  └──────────────────────────────────────────────────────────┼──────────┘   │
│                                                             │              │
│  ┌──────────────────────────────────────────────────────────▼──────────┐   │
│  │                         APPLICATION LAYER                           │   │
│  │                                                                     │   │
│  │   ┌─────────────────────────────────────────────────────────────┐   │   │
│  │   │                      TelemetryApp                           │   │   │
│  │   │                        (Façade)                             │   │   │
│  │   │                                                             │   │   │
│  │   │   • initialize()    • start()    • stop()    • mainLoop()   │   │   │
│  │   │                                                             │   │   │
│  │   └─────────────────────────────────────────────────────────────┘   │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                              │                                             │
│        ┌─────────────────────┼─────────────────────┐                       │
│        │                     │                     │                       │
│        ▼                     ▼                     ▼                       │
│  ┌───────────┐        ┌───────────┐        ┌───────────┐                   │
│  │  SOURCES  │        │ FORMATTER │        │   SINKS   │                   │
│  │   LAYER   │        │   LAYER   │        │   LAYER   │                   │
│  └─────┬─────┘        └─────┬─────┘        └─────┬─────┘                   │
│        │                    │                    │                         │
│        ▼                    ▼                    ▼                         │
│  ┌───────────┐        ┌───────────┐        ┌───────────┐                   │
│  │• File     │        │• CpuPolicy│        │• Console  │                   │
│  │• Socket   │        │• GpuPolicy│        │• File     │                   │
│  │• SOME/IP  │        │• RamPolicy│        │           │                   │
│  └───────────┘        └───────────┘        └───────────┘                   │
│                              │                                             │
│                              ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         LOGGING LAYER                               │   │
│  │                                                                     │   │
│  │   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐             │   │
│  │   │ LogManager  │───▶│ RingBuffer  │───▶│ ThreadPool  │             │   │
│  │   │             │    │             │    │             │             │   │
│  │   └─────────────┘    └─────────────┘    └─────────────┘             │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ Configuration System

### Configuration Options Comparison

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    CONFIGURATION OPTIONS                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  OPTION 1: Macros (Pre-build)                                               │
│  ════════════════════════════                                               │
│                                                                             │
│  #define SOCKET_TEL_SRC_RATE 200                                            │
│  #define ENABLE_GPU_SOURCE true                                             │
│  #define LOG_FILE_PATH "/var/log/app.log"                                   │
│                                                                             │
│  ✓ Zero runtime overhead                                                    │
│  ✓ Compiler optimizations                                                   │
│  ✗ Requires recompilation to change                                         │
│  ✗ No validation until compile time                                         │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  OPTION 2: Structs (Post-build)                                             │
│  ══════════════════════════════                                             │
│                                                                             │
│  struct CPUConfig {                                                         │
│      TelemetrySrc source = VSOMEIP;                                         │
│      int parseRate = 200;                                                   │
│  };                                                                         │
│                                                                             │
│  ✓ Type-safe                                                                │
│  ✓ IDE auto-completion                                                      │
│  ✗ Still requires recompilation                                             │
│  ✗ Configuration embedded in code                                           │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  OPTION 3: JSON Files (Runtime) ★ IMPLEMENTED                               │
│  ════════════════════════════════════════════                               │
│                                                                             │
│  {                                                                          │
│    "sources": [                                                             │
│      {"type": "file", "path": "/tmp/cpu.txt", "rateMs": 500}                │
│    ],                                                                       │
│    "sinks": [                                                               │
│      {"type": "console"}                                                    │
│    ]                                                                        │
│  }                                                                          │
│                                                                             │
│  ✓ No recompilation needed                                                  │
│  ✓ Human-readable format                                                    │
│  ✓ Easy to version control                                                  │
│  ✓ Can change configuration without code access                             │
│  ✓ Multiple configurations for different environments                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### JSON Configuration Schema

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    JSON CONFIGURATION STRUCTURE                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  {                                                                          │
│    "application": {                    ◄── Application metadata             │
│      "name": "TelemetryApp"                                                 │
│    },                                                                       │
│                                                                             │
│    "sources": [                        ◄── Array of telemetry sources       │
│      {                                                                      │
│        "type": "file",                 ◄── SourceType: file|socket|someip   │
│        "path": "/tmp/cpu_data.txt",    ◄── Path (for file/socket)           │
│        "telemetryType": "CPU",         ◄── TelemetryType: CPU|GPU|RAM       │
│        "rateMs": 500                   ◄── Polling rate in milliseconds     │
│      },                                                                     │
│      {                                                                      │
│        "type": "socket",                                                    │
│        "path": "/tmp/telemetry.sock",                                       │
│        "telemetryType": "GPU",                                              │
│        "rateMs": 300                                                        │
│      },                                                                     │
│      {                                                                      │
│        "type": "someip",               ◄── No path needed for SOME/IP       │
│        "telemetryType": "CPU",                                              │
│        "rateMs": 1000                                                       │
│      }                                                                      │
│    ],                                                                       │
│                                                                             │
│    "sinks": [                          ◄── Array of log output destinations │
│      {                                                                      │
│        "type": "console"               ◄── SinkType: console|file           │
│      },                                                                     │
│      {                                                                      │
│        "type": "file",                                                      │
│        "path": "/var/log/telemetry.log"  ◄── Path (for file sink)           │
│      }                                                                      │
│    ]                                                                        │
│  }                                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Configuration Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       JSON CONFIGURATION FLOW                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        config.json                                  │   │
│   │                                                                     │   │
│   │   {                                                                 │   │
│   │     "application": { "name": "MyApp" },                             │   │
│   │     "sources": [                                                    │   │
│   │       { "type": "file", "path": "/tmp/data.txt", ... },             │   │
│   │       { "type": "someip", ... }                                     │   │
│   │     ],                                                              │   │
│   │     "sinks": [                                                      │   │
│   │       { "type": "console" },                                        │   │
│   │       { "type": "file", "path": "/tmp/out.log" }                    │   │
│   │     ]                                                               │   │
│   │   }                                                                 │   │
│   │                                                                     │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │                                          │
│                                  │ loadConfig(path)                         │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        ConfigLoader                                 │   │
│   │                                                                     │   │
│   │   1. Open file                                                      │   │
│   │   2. Parse JSON using nlohmann/json                                 │   │
│   │   3. Extract "application" → AppConfig.appName                      │   │
│   │   4. Extract "sources" array → vector<SourceConfig>                 │   │
│   │   5. Extract "sinks" array → vector<SinkConfig>                     │   │
│   │   6. Return AppConfig struct                                        │   │
│   │                                                                     │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │                                          │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         AppConfig                                   │   │
│   │                                                                     │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │  appName: "MyApp"                                           │   │   │
│   │   │                                                             │   │   │
│   │   │  sources: [                                                 │   │   │
│   │   │    { FILE, "/tmp/data.txt", CPU, 500ms },                   │   │   │
│   │   │    { SOMEIP, "", CPU, 1000ms }                              │   │   │
│   │   │  ]                                                          │   │   │
│   │   │                                                             │   │   │
│   │   │  sinks: [                                                   │   │   │
│   │   │    { CONSOLE, "" },                                         │   │   │
│   │   │    { FILE, "/tmp/out.log" }                                 │   │   │
│   │   │  ]                                                          │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Configuration Parameters

| Parameter | Type | Description | Example Values |
|-----------|------|-------------|----------------|
| `application.name` | string | Application identifier | `"TelemetryApp"` |
| `sources[].type` | string | Source type | `"file"`, `"socket"`, `"someip"` |
| `sources[].path` | string | Source path | `"/tmp/data.txt"` |
| `sources[].telemetryType` | string | Data type | `"CPU"`, `"GPU"`, `"RAM"` |
| `sources[].rateMs` | uint32 | Polling rate (ms) | `100`, `500`, `1000` |
| `sinks[].type` | string | Sink type | `"console"`, `"file"` |
| `sinks[].path` | string | Output path | `"/var/log/app.log"` |

---

## 📦 Class Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       PHASE 6 CLASS HIERARCHY                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         ENUMERATIONS                                  │  │
│  │                                                                       │  │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │  │
│  │  │  TelemetryType  │  │   SourceType    │  │    SinkType     │        │  │
│  │  ├─────────────────┤  ├─────────────────┤  ├─────────────────┤        │  │
│  │  │ CPU             │  │ FILE            │  │ CONSOLE         │        │  │
│  │  │ GPU             │  │ SOCKET          │  │ FILE            │        │  │
│  │  │ RAM             │  │ SOMEIP          │  │                 │        │  │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘        │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      CONFIGURATION STRUCTS                            │  │
│  │                                                                       │  │
│  │  ┌─────────────────────────┐    ┌─────────────────────────┐           │  │
│  │  │     SourceConfig        │    │     SinkConfigData      │           │  │
│  │  ├─────────────────────────┤    ├─────────────────────────┤           │  │
│  │  │ + sourceType: SourceType│    │ + sinkType: SinkType    │           │  │
│  │  │ + path: string          │    │ + path: string          │           │  │
│  │  │ + telemetryType:        │    └─────────────────────────┘           │  │
│  │  │     TelemetryType       │                                          │  │
│  │  │ + rateMs: uint32_t      │                                          │  │
│  │  └─────────────────────────┘                                          │  │
│  │                                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │                        AppConfig                                │  │  │
│  │  ├─────────────────────────────────────────────────────────────────┤  │  │
│  │  │ + appName: string                                               │  │  │
│  │  │ + sources: vector<SourceConfig>                                 │  │  │
│  │  │ + sinks: vector<SinkConfigData>                                 │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                           HELPER STRUCT                               │  │
│  │                                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │                        SourceEntry                              │  │  │
│  │  ├─────────────────────────────────────────────────────────────────┤  │  │
│  │  │ + source: unique_ptr<ITelemetrySource>                          │  │  │
│  │  │ + type: TelemetryType                                           │  │  │
│  │  │ + rateMs: uint32_t                                              │  │  │
│  │  │ + lastRead: chrono::steady_clock::time_point                    │  │  │
│  │  │ + name: string                                                  │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         FAÇADE CLASS                                  │  │
│  │                                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │                      TelemetryApp                               │  │  │
│  │  │                       <<Façade>>                                │  │  │
│  │  ├─────────────────────────────────────────────────────────────────┤  │  │
│  │  │ PRIVATE MEMBERS:                                                │  │  │
│  │  │ ────────────────                                                │  │  │
│  │  │ - config_: AppConfig                                            │  │  │
│  │  │ - cpuFormatter_: unique_ptr<LogFormatter<CpuPolicy>>            │  │  │
│  │  │ - gpuFormatter_: unique_ptr<LogFormatter<GpuPolicy>>            │  │  │
│  │  │ - ramFormatter_: unique_ptr<LogFormatter<RamPolicy>>            │  │  │
│  │  │ - logManager_: unique_ptr<LogManager>                           │  │  │
│  │  │ - sinkCount_: size_t                                            │  │  │
│  │  │ - sources_: vector<SourceEntry>                                 │  │  │
│  │  │ - running_: atomic<bool>                                        │  │  │
│  │  ├─────────────────────────────────────────────────────────────────┤  │  │
│  │  │ PUBLIC METHODS:                                                 │  │  │
│  │  │ ───────────────                                                 │  │  │
│  │  │ + TelemetryApp(configPath: string)   ◄── Construct from file    │  │  │
│  │  │ + TelemetryApp(config: AppConfig)    ◄── Construct from struct  │  │  │
│  │  │ + ~TelemetryApp()                    ◄── Cleanup                │  │  │
│  │  │ + start(): void                      ◄── Run application        │  │  │
│  │  │ + stop(): void                       ◄── Stop application       │  │  │
│  │  │ + isRunning(): bool                  ◄── Check status           │  │  │
│  │  ├─────────────────────────────────────────────────────────────────┤  │  │
│  │  │ PRIVATE METHODS:                                                │  │  │
│  │  │ ────────────────                                                │  │  │
│  │  │ - initialize(): void                                            │  │  │
│  │  │ - createSources(): void                                         │  │  │
│  │  │ - createSinks(): void                                           │  │  │
│  │  │ - openSources(): void                                           │  │  │
│  │  │ - mainLoop(): void                                              │  │  │
│  │  │ - processSource(entry: SourceEntry&): void                      │  │  │
│  │  │ - formatData(data, type): optional<LogMessage>                  │  │  │
│  │  │ - printBanner(): void                                           │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  │  DELETED OPERATIONS:                                                  │  │
│  │  • Copy constructor     = delete                                      │  │
│  │  • Copy assignment      = delete                                      │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      FREE FUNCTIONS                                   │  │
│  │                                                                       │  │
│  │  + loadConfig(filePath: string): AppConfig                            │  │
│  │  + stringToTelemetryType(str: string): TelemetryType                  │  │
│  │  + stringToSourceType(str: string): SourceType                        │  │
│  │  + stringToSinkType(str: string): SinkType                            │  │
│  │  + handleSignal(sig: int): void                                       │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📝 Component Descriptions

### 1. TelemetryApp (Façade)

| Aspect | Description |
|--------|-------------|
| **Purpose** | Single entry point for entire telemetry system |
| **Pattern** | Façade |
| **Lifetime** | Manages all subsystem lifetimes |

| Constructor | Description |
|-------------|-------------|
| `TelemetryApp(string)` | Load configuration from JSON file path |
| `TelemetryApp(AppConfig)` | Use pre-built configuration struct |

| Method | Description |
|--------|-------------|
| `start()` | Open sources, setup signals, run main loop |
| `stop()` | Signal the main loop to stop |
| `isRunning()` | Check if application is currently running |

### 2. AppConfig

| Field | Type | Description |
|-------|------|-------------|
| `appName` | `string` | Application identifier |
| `sources` | `vector<SourceConfig>` | List of telemetry source configurations |
| `sinks` | `vector<SinkConfigData>` | List of sink configurations |

### 3. SourceEntry

| Field | Type | Description |
|-------|------|-------------|
| `source` | `unique_ptr<ITelemetrySource>` | Actual source instance |
| `type` | `TelemetryType` | Data type for formatter selection |
| `rateMs` | `uint32_t` | Polling interval |
| `lastRead` | `time_point` | Last read timestamp for rate limiting |
| `name` | `string` | Display name for logging |

---

## 🔄 System Flow

### High-Level Data Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         HIGH-LEVEL DATA FLOW                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        DATA SOURCES                                 │   │
│   │                                                                     │   │
│   │   ┌─────────┐      ┌─────────┐      ┌─────────┐                     │   │
│   │   │  File   │      │ Socket  │      │ SOME/IP │                     │   │
│   │   │ Source  │      │ Source  │      │ Source  │                     │   │
│   │   └────┬────┘      └────┬────┘      └────┬────┘                     │   │
│   │        │                │                │                          │   │
│   │        │   "45"         │   "78"         │   "62"                   │   │
│   │        │                │                │                          │   │
│   └────────┼────────────────┼────────────────┼──────────────────────────┘   │
│            │                │                │                              │
│            └────────────────┼────────────────┘                              │
│                             │                                               │
│                             ▼                                               │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        FORMATTERS                                   │   │
│   │                                                                     │   │
│   │        "45"                "78"               "62"                  │   │
│   │          │                   │                  │                   │   │
│   │          ▼                   ▼                  ▼                   │   │
│   │   ┌───────────┐       ┌───────────┐      ┌───────────┐              │   │
│   │   │CpuPolicy  │       │GpuPolicy  │      │CpuPolicy  │              │   │
│   │   │Formatter  │       │Formatter  │      │Formatter  │              │   │
│   │   └─────┬─────┘       └─────┬─────┘      └─────┬─────┘              │   │
│   │         │                   │                  │                    │   │
│   │         ▼                   ▼                  ▼                    │   │
│   │   LogMessage          LogMessage          LogMessage                │   │
│   │   [INFO,CPU,45%]      [WARN,GPU,78%]      [INFO,CPU,62%]            │   │
│   │                                                                     │   │
│   └────────┬────────────────────┬────────────────────┬──────────────────┘   │
│            │                    │                    │                      │
│            └────────────────────┼────────────────────┘                      │
│                                 │                                           │
│                                 ▼                                           │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        LOG MANAGER                                  │   │
│   │                                                                     │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │                    Ring Buffer                              │   │   │
│   │   │   [msg1] [msg2] [msg3] [msg4] [   ] [   ] [   ] [   ]       │   │   │
│   │   └──────────────────────────┬──────────────────────────────────┘   │   │
│   │                              │                                      │   │
│   │                        Flushing Thread                              │   │
│   │                              │                                      │   │
│   │                              ▼                                      │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │                    Thread Pool                              │   │   │
│   │   │   [Worker1] [Worker2] [Worker3] [Worker4]                   │   │   │
│   │   └──────────────────────────┬──────────────────────────────────┘   │   │
│   │                              │                                      │   │
│   └──────────────────────────────┼──────────────────────────────────────┘   │
│                                  │                                          │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                          SINKS                                      │   │
│   │                                                                     │   │
│   │        ┌─────────────────┐           ┌─────────────────┐            │   │
│   │        │   Console Sink  │           │    File Sink    │            │   │
│   │        │                 │           │                 │            │   │
│   │        │  ┌───────────┐  │           │  ┌───────────┐  │            │   │
│   │        │  │  stdout   │  │           │  │  app.log  │  │            │   │
│   │        │  └───────────┘  │           │  └───────────┘  │            │   │
│   │        └─────────────────┘           └─────────────────┘            │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Initialization Sequence

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      INITIALIZATION SEQUENCE                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   TelemetryApp app("config.json")                                           │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 1: Load Configuration                                          │   │
│   │                                                                     │   │
│   │   config.json ──▶ loadConfig() ──▶ AppConfig struct                 │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 2: Create Formatters                                           │   │
│   │                                                                     │   │
│   │   ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐       │   │
│   │   │ LogFormatter    │ │ LogFormatter    │ │ LogFormatter    │       │   │
│   │   │  <CpuPolicy>    │ │  <GpuPolicy>    │ │  <RamPolicy>    │       │   │
│   │   └─────────────────┘ └─────────────────┘ └─────────────────┘       │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 3: Create LogManager                                           │   │
│   │                                                                     │   │
│   │   LogManager(bufferSize=100, threadPoolSize=4)                      │   │
│   │         │                                                           │   │
│   │         ├──▶ Creates RingBuffer                                     │   │
│   │         ├──▶ Creates ThreadPool with 4 workers                      │   │
│   │         └──▶ Starts Flushing Thread                                 │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 4: Create Sinks                                                │   │
│   │                                                                     │   │
│   │   For each sink in config.sinks:                                    │   │
│   │                                                                     │   │
│   │   ┌─────────────┐      ┌─────────────┐                              │   │
│   │   │  CONSOLE    │ ──▶  │ new Console │ ──▶ logManager->addSink()    │   │
│   │   │  config     │      │  SinkImpl   │                              │   │
│   │   └─────────────┘      └─────────────┘                              │   │
│   │                                                                     │   │
│   │   ┌─────────────┐      ┌─────────────┐                              │   │
│   │   │   FILE      │ ──▶  │ new File    │ ──▶ logManager->addSink()    │   │
│   │   │  config     │      │  SinkImpl   │                              │   │
│   │   └─────────────┘      └─────────────┘                              │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 5: Create Sources                                              │   │
│   │                                                                     │   │
│   │   For each source in config.sources:                                │   │
│   │                                                                     │   │
│   │   ┌─────────────┐                                                   │   │
│   │   │   FILE      │ ──▶ new FileTelemetrySourceImpl(path)             │   │
│   │   │  config     │     + SourceEntry{ type, rateMs, lastRead }       │   │
│   │   └─────────────┘                                                   │   │
│   │                                                                     │   │
│   │   ┌─────────────┐                                                   │   │
│   │   │  SOCKET     │ ──▶ new SocketTelemetrySourceImpl(path)           │   │
│   │   │  config     │     + SourceEntry{ type, rateMs, lastRead }       │   │
│   │   └─────────────┘                                                   │   │
│   │                                                                     │   │
│   │   ┌─────────────┐                                                   │   │
│   │   │  SOMEIP     │ ──▶ new SomeIPTelemetrySourceAdapter()            │   │
│   │   │  config     │     + SourceEntry{ type, rateMs, lastRead }       │   │
│   │   └─────────────┘                                                   │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ INITIALIZATION COMPLETE                                             │   │
│   │                                                                     │   │
│   │   TelemetryApp is ready to start()                                  │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔁 Main Loop Flow

### Processing Loop Detail

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          MAIN LOOP FLOW                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   app.start()                                                               │
│         │                                                                   │
│         ├──▶ Setup signal handlers (SIGINT, SIGTERM)                        │
│         ├──▶ Open all sources (source->openSource())                        │
│         ├──▶ Set running_ = true                                            │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         MAIN LOOP                                   │   │
│   │                                                                     │   │
│   │   while (running_ && !stopRequested) {                              │   │
│   │                                                                     │   │
│   │             ┌─────────────────────────────────────────────────┐     │   │
│   │             │         FOR EACH SOURCE                         │     │   │
│   │             │                                                 │     │   │
│   │             │   now = current_time()                          │     │   │
│   │             │         │                                       │     │   │
│   │             │         ▼                                       │     │   │
│   │             │   elapsed = now - source.lastRead               │     │   │
│   │             │         │                                       │     │   │
│   │             │         ▼                                       │     │   │
│   │             │   ┌──────────────────────────────────────┐      │     │   │
│   │             │   │     elapsed >= source.rateMs ?       │      │     │   │
│   │             │   └───────────────┬──────────────────────┘      │     │   │
│   │             │                   │                             │     │   │
│   │             │         YES       │        NO                   │     │   │
│   │             │           │       │         │                   │     │   │
│   │             │           ▼       │         ▼                   │     │   │
│   │             │   ┌───────────┐   │   Skip this source          │     │   │
│   │             │   │ process   │   │                             │     │   │
│   │             │   │ Source()  │   │                             │     │   │
│   │             │   └───────────┘   │                             │     │   │
│   │             │                                                 │     │   │
│   │             └─────────────────────────────────────────────────┘     │   │
│   │                                                                     │   │
│   │             sleep(10ms)  // Prevent busy-waiting                    │   │
│   │                                                                     │   │
│   │   }                                                                 │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Process Source Detail

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       PROCESS SOURCE DETAIL                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   processSource(entry)                                                      │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 1: Read Data                                                   │   │
│   │                                                                     │   │
│   │   source->readSource(data)                                          │   │
│   │         │                                                           │   │
│   │         ▼                                                           │   │
│   │   data = "45"  (raw telemetry value)                                │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 2: Format Data                                                 │   │
│   │                                                                     │   │
│   │   formatData(data, type)                                            │   │
│   │         │                                                           │   │
│   │         ├── type == CPU ──▶ cpuFormatter->formatDataToLogMsg()      │   │
│   │         ├── type == GPU ──▶ gpuFormatter->formatDataToLogMsg()      │   │
│   │         └── type == RAM ──▶ ramFormatter->formatDataToLogMsg()      │   │
│   │                   │                                                 │   │
│   │                   ▼                                                 │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │                    LogMessage                               │   │   │
│   │   │                                                             │   │   │
│   │   │   appName:  "TestApp"                                       │   │   │
│   │   │   context:  "CPU"                                           │   │   │
│   │   │   severity: "INFO"                                          │   │   │
│   │   │   time:     "2024-01-15 10:30:00"                           │   │   │
│   │   │   message:  "CPU usage: 45.00%"                             │   │   │
│   │   │                                                             │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 3: Log Message                                                 │   │
│   │                                                                     │   │
│   │   logManager->log(logMessage)                                       │   │
│   │         │                                                           │   │
│   │         ├──▶ Push to RingBuffer                                     │   │
│   │         └──▶ Notify flushing thread                                 │   │
│   │                                                                     │   │
│   │         (Async: ThreadPool workers write to sinks)                  │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 4: Update Timing                                               │   │
│   │                                                                     │   │
│   │   entry.lastRead = now                                              │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Rate Limiting Mechanism

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      RATE LIMITING MECHANISM                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Timeline for Source with rateMs = 500:                                     │
│  ══════════════════════════════════════                                     │
│                                                                             │
│  Time(ms): 0    100   200   300   400   500   600   700   800   900   1000  │
│            │     │     │     │     │     │     │     │     │     │     │    │
│            ▼     ▼     ▼     ▼     ▼     ▼     ▼     ▼     ▼     ▼     ▼    │
│           READ  skip  skip  skip  skip  READ  skip  skip  skip  skip  READ  │
│            │                             │                             │    │
│            └─── 500ms elapsed ───────────┘─── 500ms elapsed ───────────┘    │
│                                                                             │
│  Multiple Sources with Different Rates:                                     │
│  ═══════════════════════════════════════                                    │
│                                                                             │
│  CPU Source (rateMs=500):  READ──────────────READ──────────────READ         │
│  GPU Source (rateMs=300):  READ────────READ────────READ────────READ         │
│  RAM Source (rateMs=1000): READ─────────────────────────────────────READ    │
│                                                                             │
│  Time(ms): 0   100  200  300  400  500  600  700  800  900  1000            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🛑 Shutdown Sequence

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SHUTDOWN SEQUENCE                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   User presses Ctrl+C                                                       │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 1: Signal Handler                                              │   │
│   │                                                                     │   │
│   │   SIGINT received ──▶ handleSignal()                                │   │
│   │                             │                                       │   │
│   │                             ▼                                       │   │
│   │                      g_stopRequested = 1                            │   │
│   │                                                                     │   |
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 2: Main Loop Exits                                             │   │
│   │                                                                     │   │
│   │   while (running_ && !stopRequested)  ──▶  condition FALSE          │   │
│   │                                                                     │   │
│   │   Loop exits naturally                                              │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ STEP 3: Destructor Called (~TelemetryApp)                           │   │
│   │                                                                     │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │  3.1: Set running_ = false                                  │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                       │                                             │   │
│   │                       ▼                                             │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │  3.2: Wait for main loop to fully exit                      │   │   │
│   │   │       sleep(100ms)                                          │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                       │                                             │   │
│   │                       ▼                                             │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │  3.3: Clear sources                                         │   │   │
│   │   │       sources_.clear()                                      │   │   │
│   │   │       (Destroys source unique_ptrs, closes files/sockets)   │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                       │                                             │   │
│   │                       ▼                                             │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │  3.4: Clear formatters                                      │   │   │
│   │   │       cpuFormatter_.reset()                                 │   │   │
│   │   │       gpuFormatter_.reset()                                 │   │   │
│   │   │       ramFormatter_.reset()                                 │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                       │                                             │   │
│   │                       ▼                                             │   │
│   │   ┌─────────────────────────────────────────────────────────────┐   │   │
│   │   │  3.5: Reset LogManager                                      │   │   │
│   │   │       logManager_.reset()                                   │   │   │
│   │   │                                                             │   │   │
│   │   │       LogManager destructor:                                │   │   │
│   │   │         ├── Stop flushing thread                            │   │   │
│   │   │         ├── Join flushing thread                            │   │   │
│   │   │         ├── ThreadPool destructor                           │   │   │
│   │   │         │     ├── Signal workers to stop                    │   │   │
│   │   │         │     └── Join all worker threads                   │   │   │
│   │   │         └── Delete all sinks                                │   │   │
│   │   │                                                             │   │   │
│   │   └─────────────────────────────────────────────────────────────┘   │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│         │                                                                   │
│         ▼                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ SHUTDOWN COMPLETE                                                   │   │
│   │                                                                     │   │
│   │   All resources released, all threads joined                        │   │
│   │   Program exits cleanly                                             │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Thread Termination Order

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     THREAD TERMINATION ORDER                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   BEFORE SHUTDOWN:                                                          │
│   ════════════════                                                          │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                                                                     │   │
│   │   [Main Thread]     ──▶ Running mainLoop()                          │   │
│   │                                                                     │   │
│   │   [Flushing Thread] ──▶ Waiting on condition variable               │   │
│   │                                                                     │   │
│   │   [Worker Thread 1] ──▶ Waiting for tasks                           │   │
│   │   [Worker Thread 2] ──▶ Waiting for tasks                           │   │
│   │   [Worker Thread 3] ──▶ Waiting for tasks                           │   │
│   │   [Worker Thread 4] ──▶ Waiting for tasks                           │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   SHUTDOWN ORDER:                                                           │
│   ═══════════════                                                           │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                                                                     │   │
│   │   1. [Main Thread]      ◀── Exits mainLoop() (g_stopRequested = 1)  │   │
│   │         │                                                           │   │
│   │         │ calls ~TelemetryApp()                                     │   │
│   │         │                                                           │   │
│   │         │ calls logManager_.reset()                                 │   │
│   │         │                                                           │   │
│   │         ▼                                                           │   │
│   │   2. [Flushing Thread]  ◀── Signaled, joined, terminated            │   │
│   │         │                                                           │   │
│   │         │ ~ThreadPool() called                                      │   │
│   │         │                                                           │   │
│   │         ▼                                                           │   │
│   │   3. [Worker Thread 1]  ◀── Signaled, joined, terminated            │   │
│   │   4. [Worker Thread 2]  ◀── Signaled, joined, terminated            │   │
│   │   5. [Worker Thread 3]  ◀── Signaled, joined, terminated            │   │
│   │   6. [Worker Thread 4]  ◀── Signaled, joined, terminated            │   │
│   │                                                                     │   │
│   │         │                                                           │   │
│   │         │ All threads joined                                        │   │
│   │         │                                                           │   │
│   │         ▼                                                           │   │
│   │   7. [Main Thread]      ◀── Continues, destroys sinks, exits main() │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Build & Run Instructions

### Prerequisites

| Requirement | Version | Description |
|-------------|---------|-------------|
| CMake | ≥ 3.10 | Build system |
| C++ Compiler | C++17 | Required for `std::optional`, structured bindings |
| nlohmann/json | Any | Single header JSON library (included in third_party/) |

### Build Commands

```bash
# Navigate to project root
cd TELEMETRY_AND_LOGGING_SYSTEM

# Configure and build (without SOME/IP)
cmake -S ./examples -B build -DApp_Source_File=phase6_demo.cpp
cmake --build build

# Configure and build (with SOME/IP support)
cmake -S ./examples -B build -DApp_Source_File=phase6_demo.cpp -DENABLE_SOMEIP=ON
cmake --build build

# Run
./build/Demo

# Run with custom config
./build/Demo /path/to/my_config.json
```

### CMake Options

| Option | Default | Description |
|--------|---------|-------------|
| `App_Source_File` | - | Demo file to build |
| `ENABLE_SOMEIP` | OFF | Enable SOME/IP telemetry source |

---

## 💻 Usage Examples

### Minimal Example

```cpp
#include "app/TelemetryApp.hpp"

int main() {
    telemetry::TelemetryApp app("/path/to/config.json");
    app.start();  // Runs until Ctrl+C
    return 0;
}
```

### Programmatic Configuration

```cpp
#include "app/TelemetryApp.hpp"

int main() {
    telemetry::AppConfig config;
    config.appName = "MyTelemetryApp";
    
    // Add a CPU source
    telemetry::SourceConfig cpuSource;
    cpuSource.sourceType = telemetry::SourceType::FILE;
    cpuSource.path = "/tmp/cpu_data.txt";
    cpuSource.telemetryType = telemetry::TelemetryType::CPU;
    cpuSource.rateMs = 500;
    config.sources.push_back(cpuSource);
    
    // Add console sink
    telemetry::SinkConfigData consoleSink;
    consoleSink.sinkType = telemetry::SinkType::CONSOLE;
    config.sinks.push_back(consoleSink);
    
    telemetry::TelemetryApp app(config);
    app.start();
    return 0;
}
```

### Sample Configuration File

```json
{
    "application": {
        "name": "ProductionTelemetryApp"
    },
    "sources": [
        {
            "type": "file",
            "path": "/tmp/cpu_data.txt",
            "telemetryType": "CPU",
            "rateMs": 500
        },
        {
            "type": "file",
            "path": "/tmp/gpu_data.txt",
            "telemetryType": "GPU",
            "rateMs": 300
        },
        {
            "type": "socket",
            "path": "/tmp/telemetry.sock",
            "telemetryType": "RAM",
            "rateMs": 1000
        }
    ],
    "sinks": [
        {
            "type": "console"
        },
        {
            "type": "file",
            "path": "/var/log/telemetry.log"
        }
    ]
}
```

### Expected Output

```
╔═══════════════════════════════════════╗
║      PHASE 6 DEMO: TelemetryApp       ║
╚═══════════════════════════════════════╝

[Demo] Created test data files
[Demo] Created test config
[App] Initializing...
[App] + File source: /tmp/cpu_data.txt
[App] + File source: /tmp/gpu_data.txt
[App] Total sources: 2
[App] Initialized

========================================
  TestApp
========================================
  Sources: 2
  Sinks:   2
========================================

[App] ✓ Opened: File[/tmp/cpu_data.txt]
[App] ✓ Opened: File[/tmp/gpu_data.txt]
[App] Running... (Ctrl+C to stop)
--------------------------------------------------
[2024-06-15 10:30:00] <INFO> (TestApp - CPU) : CPU usage at 45.00% - Normal operation
[2024-06-15 10:30:00] <INFO> (TestApp - GPU) : GPU usage at 30.00% - Normal operation
[2024-06-15 10:30:00] <WARNING> (TestApp - CPU) : CPU usage at 67.00% - Approaching high usage
[2024-06-15 10:30:01] <CRITICAL> (TestApp - GPU) : GPU usage at 88.00% - Critical level reached
[2024-06-15 10:30:01] <CRITICAL> (TestApp - CPU) : CPU usage at 91.00% - Critical level reached
^C
[App] Stopped
[App] Cleaning up...
[App] Cleanup complete
[Demo] Done!
```

---

## 🎓 Key Learning Points

### 1. Façade Pattern Implementation

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    FAÇADE PATTERN KEY POINTS                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  BEFORE FAÇADE:                                                             │
│  ══════════════                                                             │
│                                                                             │
│  int main() {                                                               │
│      // 50+ lines of setup code                                             │
│      auto cpuFormatter = make_unique<LogFormatter<CpuPolicy>>("App");       │
│      auto gpuFormatter = make_unique<LogFormatter<GpuPolicy>>("App");       │
│      auto ramFormatter = make_unique<LogFormatter<RamPolicy>>("App");       │
│      auto logManager = make_unique<LogManager>(100, 4);                     │
│      auto consoleSink = new ConsoleSinkImpl();                              │
│      auto fileSink = new FileSinkImpl("/tmp/log.txt");                      │
│      logManager->addSink(consoleSink);                                      │
│      logManager->addSink(fileSink);                                         │
│      auto fileSource = make_unique<FileTelemetrySourceImpl>("/tmp/data");   │
│      fileSource->openSource();                                              │
│      // ... signal handling ...                                             │
│      // ... main loop with rate limiting ...                                │
│      // ... cleanup ...                                                     │
│  }                                                                          │
│                                                                             │
│  AFTER FAÇADE:                                                              │
│  ═════════════                                                              │
│                                                                             │
│  int main() {                                                               │
│      TelemetryApp app("config.json");  // 1 line                            │
│      app.start();                       // 1 line                           │
│      return 0;                                                              │
│  }                                                                          │
│                                                                             │
│  COMPLEXITY HIDDEN:                                                         │
│  ══════════════════                                                         │
│  • Configuration loading and parsing                                        │
│  • Object creation and wiring                                               │
│  • Resource management                                                      │
│  • Signal handling                                                          │
│  • Main loop with rate limiting                                             │
│  • Graceful shutdown                                                        │
│  • Error handling                                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2. JSON Configuration Benefits

| Benefit | Description |
|---------|-------------|
| **Runtime Flexibility** | Change configuration without recompilation |
| **Environment-Specific** | Different configs for dev/staging/production |
| **Human-Readable** | Easy to understand and modify |
| **Version Control Friendly** | Track configuration changes in git |
| **Validation** | Can validate JSON schema before running |
| **Documentation** | Config file serves as documentation |

### 3. Signal Handling for Graceful Shutdown

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    SIGNAL HANDLING MECHANISM                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  SETUP:                                                                     │
│  ══════                                                                     │
│                                                                             │
│  static volatile std::sig_atomic_t g_stopRequested = 0;                     │
│                                                                             │
│  void handleSignal(int sig) {                                               │
│      (void)sig;              // Suppress unused warning                     │
│      g_stopRequested = 1;    // Signal-safe flag set                        │
│  }                                                                          │
│                                                                             │
│  // In start():                                                             │
│  std::signal(SIGINT, handleSignal);   // Ctrl+C                             │
│  std::signal(SIGTERM, handleSignal);  // kill command                       │
│                                                                             │
│  FLOW:                                                                      │
│  ═════                                                                      │
│                                                                             │
│  User presses Ctrl+C                                                        │
│       │                                                                     │
│       ▼                                                                     │
│  OS sends SIGINT to process                                                 │
│       │                                                                     │
│       ▼                                                                     │
│  handleSignal() called (interrupts main thread)                             │
│       │                                                                     │
│       ▼                                                                     │
│  g_stopRequested = 1                                                        │
│       │                                                                     │
│       ▼                                                                     │
│  mainLoop() checks flag on next iteration                                   │
│       │                                                                     │
│       ▼                                                                     │
│  while (running_ && g_stopRequested == 0)  ◄── Condition false!             │
│       │                                                                     │
│       ▼                                                                     │
│  Loop exits, cleanup begins                                                 │
│                                                                             │
│  WHY volatile sig_atomic_t?                                                 │
│  ══════════════════════════                                                 │
│  • volatile: Compiler won't optimize away reads                             │
│  • sig_atomic_t: Guaranteed atomic read/write in signal handler             │
│  • Prevents undefined behavior from concurrent access                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4. Atomic Boolean for Thread Safety

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    ATOMIC BOOLEAN USAGE                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  std::atomic<bool> running_{false};                                         │
│                                                                             │
│  WHY ATOMIC?                                                                │
│  ══════════                                                                 │
│                                                                             │
│  Thread 1 (main loop):              Thread 2 (signal or stop()):            │
│  ═════════════════════              ═══════════════════════════             │
│                                                                             │
│  while (running_.load()) {          void stop() {                           │
│      // do work                         running_.store(false);              │
│  }                                  }                                       │
│                                                                             │
│  WITHOUT atomic:                                                            │
│  • Compiler might cache running_ in register                                │
│  • Thread 1 might never see Thread 2's write                                │
│  • Undefined behavior (data race)                                           │
│                                                                             │
│  WITH atomic:                                                               │
│  • Guaranteed visibility across threads                                     │
│  • Memory ordering ensures proper synchronization                           │
│  • No data race                                                             │
│                                                                             │
│  METHODS:                                                                   │
│  ════════                                                                   │
│  • running_.store(true)   - Atomic write                                    │
│  • running_.load()        - Atomic read                                     │
│  • running_ = true        - Also atomic (operator overload)                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5. Conditional Compilation for Optional Features

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    CONDITIONAL COMPILATION                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  CMakeLists.txt:                                                            │
│  ═══════════════                                                            │
│                                                                             │
│  option(ENABLE_SOMEIP "Enable SOME/IP source" OFF)                          │
│  if(ENABLE_SOMEIP)                                                          │
│      target_compile_definitions(${PROJECT_NAME} PRIVATE SOMEIP_ENABLED)     │
│  endif()                                                                    │
│                                                                             │
│  Source Code:                                                               │
│  ════════════                                                               │
│                                                                             │
│  #ifdef SOMEIP_ENABLED                                                      │
│  #include "sources/SomeIPTelemetrySourceAdapter.hpp"                        │
│  #endif                                                                     │
│                                                                             │
│  case SourceType::SOMEIP:                                                   │
│  #ifdef SOMEIP_ENABLED                                                      │
│      entry.source = std::make_unique<SomeIPTelemetrySourceAdapter>();       │
│      entry.name = "SOME/IP";                                                │
│  #else                                                                      │
│      std::cout << "[App] ! SOME/IP not enabled, skipping" << std::endl;     │
│      continue;  // Skip this source                                         │
│  #endif                                                                     │
│      break;                                                                 │
│                                                                             │
│  BUILD VARIATIONS:                                                          │
│  ═════════════════                                                          │
│                                                                             │
│  # Without SOME/IP (smaller binary, no vsomeip dependency)                  │
│  cmake -S . -B build                                                        │
│                                                                             │
│  # With SOME/IP (full functionality)                                        │
│  cmake -S . -B build -DENABLE_SOMEIP=ON                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## ⚠️ Common Pitfalls

| Pitfall | Problem | Solution |
|---------|---------|----------|
| Missing config file | `runtime_error` thrown | Check file exists before passing to constructor |
| Invalid JSON syntax | Parse error | Validate JSON with online tool or `try-catch` |
| Wrong source type string | Falls back to FILE | Use exact strings: `"file"`, `"socket"`, `"someip"` |
| File source path doesn't exist | `openSource()` fails | Create test files or check paths |
| Socket server not running | Connection fails | Start socket server before client |
| SOME/IP not enabled | Source skipped silently | Build with `-DENABLE_SOMEIP=ON` |
| Rate too low | High CPU usage | Use reasonable rates (100ms minimum) |
| Forgetting Ctrl+C | App runs forever | Signal handlers installed for graceful exit |
| Memory leaks | Improper cleanup | `unique_ptr` and RAII handle this automatically |

---

## 📁 File Structure

```
Phase 6 Files:
├── include/
│   └── app/
│       ├── AppConfig.hpp           # Configuration structures & enums
│       └── TelemetryApp.hpp        # Façade class declaration
│
├── src/
│   └── app/
│       ├── AppConfig.cpp           # JSON loading implementation
│       ├── TelemetryApp.cpp        # Façade class implementation
│       └── CMakeLists.txt          # Build configuration
│
├── third_party/
│   └── json.hpp                    # nlohmann/json single header
│
├── examples/
│   └── phase6_demo.cpp             # Demo application
│
└── config/
    └── telemetry_config.json       # Sample configuration file
```

---

## 🔗 Complete System Integration

### All Phases Combined

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                 COMPLETE SYSTEM INTEGRATION (ALL PHASES)                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    PHASE 6: TelemetryApp (Façade)                     │  │
│  │                                                                       │  │
│  │   ┌─────────────────────────────────────────────────────────────┐     │  │
│  │   │  config.json ──▶ loadConfig() ──▶ AppConfig                 │     │  │
│  │   │                                                             │     │  │
│  │   │  TelemetryApp(configPath)                                   │     │  │
│  │   │       │                                                     │     │  │
│  │   │       └──▶ initialize()                                     │     │  │
│  │   │             start()                                         │     │  │
│  │   │             mainLoop()                                      │     │  │
│  │   │             ~TelemetryApp()                                 │     │  │
│  │   └─────────────────────────────────────────────────────────────┘     │  │
│  │                              │                                        │  │
│  └──────────────────────────────┼────────────────────────────────────────┘  │
│                                 │                                           │
│           ┌─────────────────────┼─────────────────────┐                     │
│           │                     │                     │                     │
│           ▼                     ▼                     ▼                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐  │
│  │   PHASE 2/5:    │  │    PHASE 3:     │  │      PHASE 1/4:             │  │
│  │    Sources      │  │   Formatters    │  │    Logging System           │  │
│  │                 │  │                 │  │                             │  │
│  │ • FileTelemetry │  │ • LogFormatter  │  │ • LogManager (async)        │  │
│  │   SourceImpl    │  │   <CpuPolicy>   │  │ • RingBuffer                │  │
│  │ • SocketTelemetry│ │   <GpuPolicy>   │  │ • ThreadPool                │  │
│  │   SourceImpl    │  │   <RamPolicy>   │  │ • ConsoleSinkImpl           │  │
│  │ • SomeIPTelemetry│ │                 │  │ • FileSinkImpl              │  │
│  │   SourceAdapter │  │ Policy-based    │  │                             │  │
│  │                 │  │ severity calc   │  │ Async flush to sinks        │  │
│  └────────┬────────┘  └────────┬────────┘  └──────────────┬──────────────┘  │
│           │                    │                          │                 │
│           │    ITelemetrySource│                          │                 │
│           │    interface       │                          │                 │
│           │                    │                          │                 │
│           ▼                    ▼                          ▼                 │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                         DATA FLOW                                    │   │
│  │                                                                      │   │
│  │  Source.readSource() ──▶ "45" ──▶ Formatter.formatDataToLogMsg()     │   │
│  │                                              │                       │   │
│  │                                              ▼                       │   │
│  │                                       LogMessage {                   │   │
│  │                                         appName: "TestApp",          │   │
│  │                                         context: "CPU",              │   │
│  │                                         severity: "INFO",            │   │
│  │                                         time: "2024-...",            │   │
│  │                                         message: "45%"               │   │
│  │                                       }                              │   │
│  │                                              │                       │   │
│  │                                              ▼                       │   │
│  │                              LogManager.log(msg) ──▶ RingBuffer      │   │
│  │                                                          │           │   │
│  │                                                          ▼           │   │
│  │                                               FlushingThread         │   │
│  │                                                          │           │   │
│  │                                                          ▼           │   │
│  │                                                    ThreadPool        │   │
│  │                                                    │        │        │   │
│  │                                                    ▼        ▼        │   │
│  │                                               Console    File        │   │
│  │                                                                      │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Ownership Hierarchy

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    OWNERSHIP HIERARCHY                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  TelemetryApp                                                               │
│  │                                                                          │
│  ├──▶ config_ : AppConfig                        (value - copied)           │
│  │                                                                          │
│  ├──▶ cpuFormatter_ : unique_ptr<LogFormatter<CpuPolicy>>                   │
│  │    └──▶ LogFormatter (owns nothing)                                      │
│  │                                                                          │
│  ├──▶ gpuFormatter_ : unique_ptr<LogFormatter<GpuPolicy>>                   │
│  │    └──▶ LogFormatter (owns nothing)                                      │
│  │                                                                          │
│  ├──▶ ramFormatter_ : unique_ptr<LogFormatter<RamPolicy>>                   │
│  │    └──▶ LogFormatter (owns nothing)                                      │
│  │                                                                          │
│  ├──▶ logManager_ : unique_ptr<LogManager>                                  │
│  │    ├──▶ RingBuffer<LogMessage>                                           │
│  │    ├──▶ ThreadPool                                                       │
│  │    │    └──▶ vector<thread> (workers)                                    │
│  │    ├──▶ thread (flushingThread)                                          │
│  │    └──▶ vector<ILogSink*>            ◄── LogManager OWNS these!          │
│  │         ├──▶ ConsoleSinkImpl*                                            │
│  │         └──▶ FileSinkImpl*                                               │
│  │              └──▶ ofstream (file handle)                                 │
│  │                                                                          │
│  └──▶ sources_ : vector<SourceEntry>                                        │
│       └──▶ SourceEntry                                                      │
│            └──▶ source : unique_ptr<ITelemetrySource>                       │
│                 │                                                           │
│                 ├──▶ FileTelemetrySourceImpl                                │
│                 │    └──▶ _safeFilePtr : unique_ptr<SafeFile>               │
│                 │         └──▶ fd : int (file descriptor)                   │
│                 │                                                           │
│                 ├──▶ SocketTelemetrySourceImpl                              │
│                 │    └──▶ _safeSocketPtr : unique_ptr<SafeSocket>           │
│                 │         └──▶ SocketFd : int (socket descriptor)           │
│                 │                                                           │
│                 └──▶ SomeIPTelemetrySourceAdapter                           │
│                      └──▶ impl_ : SomeIPTelemetrySourceImpl& (singleton)    │
│                           └──▶ proxy_ : shared_ptr<TelemetryServiceProxy>   │
│                                                                             │
│  DESTRUCTION ORDER (reverse of construction):                               │
│  ════════════════════════════════════════════                               │
│  1. running_ = false                                                        │
│  2. sources_.clear()     → SafeFile/SafeSocket destructors close fds        │
│  3. formatters_.reset()  → Formatter destructors (trivial)                  │
│  4. logManager_.reset()  → ThreadPool stops, sinks deleted, files closed    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Summary

### Phase Comparison Table

| Phase | Focus | Key Pattern/Concept |
|-------|-------|---------------------|
| **1** | Core Logging | Strategy Pattern, Virtual Functions, ILogSink, LogManager, Polymorphism |
| **2** | Data Sources | RAII, Move Semantics, Rule of 5, SafeFile, SafeSocket, ITelemetrySource |
| **3** | Policy-Based Design | Templates, Factory, Builder, LogFormatter\<Policy\>, Ring Buffer |
| **4** | Async Logging | Thread Pool, Condition Variables, Thread-Safe Ring Buffer, Producer-Consumer |
| **5** | Network Telemetry | Singleton, Adapter Pattern, SOME/IP, CommonAPI, Franca IDL |
| **6** | System Integration | Façade Pattern, JSON Configuration, Signal Handling, Graceful Shutdown |

### Design Patterns Summary

| Pattern | Phase | Implementation | Purpose |
|---------|-------|----------------|---------|
| Strategy | 1 | ILogSink | Interchangeable sinks |
| Template Method | 3 | LogFormatter\<Policy\> | Compile-time config |
| Factory | 3 | LogSinkFactory | Create sinks |
| Builder | 3 | LogManagerBuilder | Fluent construction |
| Singleton | 5 | SomeIPTelemetrySourceImpl | Single SOME/IP client |
| Adapter | 5 | SomeIPTelemetrySourceAdapter | Interface adaptation |
| Proxy | 5 | TelemetryServiceProxy (generated) | Remote service access |
| **Façade** | **6** | **TelemetryApp** | **Simplified interface** |

### C++ Features by Phase

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    C++ FEATURES BY PHASE                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Phase 1: Fundamentals                                                      │
│  ═════════════════════                                                      │
│  • virtual, override, = default, = delete                                   │
│  • Inheritance and polymorphism                                             │
│  • std::vector, raw pointers                                                │
│                                                                             │
│  Phase 2: Resource Management                                               │
│  ═══════════════════════════                                                │
│  • RAII idiom                                                               │
│  • Move semantics (&&, std::move)                                           │
│  • Rule of 3/5/0                                                            │
│  • std::unique_ptr                                                          │
│  • POSIX file/socket APIs                                                   │
│                                                                             │
│  Phase 3: Generic Programming                                               │
│  ════════════════════════════                                               │
│  • Templates (class and function)                                           │
│  • Policy-based design                                                      │
│  • std::optional                                                            │
│  • enum class                                                               │
│  • Static members                                                           │
│                                                                             │
│  Phase 4: Concurrency                                                       │
│  ════════════════════                                                       │
│  • std::thread                                                              │
│  • std::mutex, std::lock_guard, std::unique_lock                            │
│  • std::condition_variable                                                  │
│  • std::atomic                                                              │
│  • std::function, lambdas                                                   │
│                                                                             │
│  Phase 5: External Libraries                                                │
│  ══════════════════════════                                                 │
│  • CommonAPI/vsomeip integration                                            │
│  • Code generation from IDL                                                 │
│  • std::shared_ptr for library objects                                      │
│  • Static local singleton (thread-safe since C++11)                         │
│                                                                             │
│  Phase 6: Application Architecture                                          │
│  ═════════════════════════════════                                          │
│  • JSON parsing (nlohmann/json)                                             │
│  • Signal handling (std::signal)                                            │
│  • std::chrono for timing                                                   │
│  • Façade pattern for API design                                            │
│  • Conditional compilation (#ifdef)                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📚 Further Reading

| Topic | Resource |
|-------|----------|
| Façade Pattern | refactoring.guru/design-patterns/facade |
| nlohmann/json | github.com/nlohmann/json |
| Signal Handling | man7.org/linux/man-pages/man2/signal.2.html |
| std::atomic | cppreference.com/w/cpp/atomic/atomic |
| std::chrono | cppreference.com/w/cpp/chrono |
| RAII | cppreference.com/w/cpp/language/raii |

---

## 🎉 Project Complete!

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ╔═══════════════════════════════════════════════════════════════════╗     │
│   ║                                                                   ║     │
│   ║          TELEMETRY AND LOGGING SYSTEM - COMPLETE!                 ║     │
│   ║                                                                   ║     │
│   ║   Phase 1: ✓ Core Synchronous Logging                             ║     │
│   ║   Phase 2: ✓ Data Sources & RAII                                  ║     │
│   ║   Phase 3: ✓ Policy-Based Design                                  ║     │
│   ║   Phase 4: ✓ Asynchronous Logging                                 ║     │
│   ║   Phase 5: ✓ Network Telemetry (SOME/IP)                          ║     │
│   ║   Phase 6: ✓ System Integration (Façade)                          ║     │
│   ║                                                                   ║     │
│   ║   The system now provides:                                        ║     │
│   ║   • Multiple telemetry sources (File, Socket, SOME/IP)            ║     │
│   ║   • Policy-based severity classification                          ║     │
│   ║   • Asynchronous, non-blocking logging                            ║     │
│   ║   • Multiple output sinks (Console, File)                         ║     │
│   ║   • JSON-based runtime configuration                              ║     │
│   ║   • Clean, simple API via Façade pattern                          ║     │
│   ║                                                                   ║     │
│   ╚═══════════════════════════════════════════════════════════════════╝     │
│                                                                             │
│   Usage:                                                                    │
│   ═══════                                                                   │
│                                                                             │
│   TelemetryApp app("config.json");                                          │
│   app.start();                                                              │
│                                                                             │
│   That's it! 🎉                                                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```